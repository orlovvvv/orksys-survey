Implement the following plan:

# Phase 4: Analytics & Insights Implementation Plan

## Context

This plan implements Phase 4 of the survey platform, adding comprehensive analytics dashboard and AI-powered insights. The current system has:
- Survey builder and runner fully functional (Phases 1-3 complete)
- Basic `response.getStats` endpoint returning `{ total, complete, partial }`
- Minimal dashboard showing only API status and subscription info
- No chart library or analytics visualization components

The goal is to enable analysts to view survey results at a glance and leverage AI to find patterns in text responses.

---

## Implementation Overview

### New Dependencies
```bash
pnpm add recharts --filter @orksys-survey/web    # Chart library
pnpm add @tanstack/react-table --filter @orksys-survey/web  # Response table
pnpm add openai --filter @orksys-survey/api     # Groq (OpenAI-compatible) client
```

### Environment Variables (for AI Insights)
```
GROQ_API_KEY=<your-groq-api-key>
GROQ_MODEL=llama-3.3-70b-versatile  # or mixtral-8x7b-32768, llama-3.1-8b-instant
```

> Note: Groq uses OpenAI-compatible API. The service layer will use `openai` SDK with `baseURL: "https://api.groq.com/openai/v1"`.

---

## 1. Database Schema Extensions

**File:** `packages/db/src/schema/analytics.ts` (NEW)

Create tables for caching analytics computations and AI analysis results:

- `analyticsSnapshot` - Pre-computed analytics with 5-minute TTL
- `aiAnalysis` - LLM-generated insights with 24-hour TTL

**File:** `packages/db/src/schema/survey.ts` (MODIFY)

Add composite indexes for efficient aggregation queries:
- `(surveyId, completedAt)` for time-series
- `(surveyId, isComplete)` for completion stats
- `(questionId, responseId)` for answer aggregations

---

## 2. API Layer

### 2.1 Analytics Router

**File:** `packages/api/src/routers/analytics.ts` (NEW)

| Endpoint | Description |
|----------|-------------|
| `getSummary` | Overall survey stats (responses, completion rate, avg time) |
| `getQuestionAnalytics` | Per-question distributions (choice/numeric/text) |
| `getTrends` | Time-series data for response trends |
| `getDemographics` | Breakdown by device, browser, country |
| `exportResponses` | CSV/JSON export of responses |

**Patterns to follow:**
- Use `organizationProcedure` for access control
- Use existing `verifySurveyAccess()` helper
- Parallel queries with `Promise.all()`
- Cache results in `analyticsSnapshot` table

### 2.2 AI Insights Router

**File:** `packages/api/src/routers/insights.ts` (NEW)

| Endpoint | Description |
|----------|-------------|
| `getTextSummary` | 3-bullet AI summary of open-ended responses |
| `getSentimentAnalysis` | Sentiment scores (positive/neutral/negative %) |
| `getKeywords` | Extracted keyword cloud data |
| `refreshAnalysis` | Force re-analysis (admin only) |

### 2.3 Router Registration

**File:** `packages/api/src/routers/index.ts` (MODIFY)

```typescript
import { analyticsRouter } from "./analytics";
import { insightsRouter } from "./insights";

export const appRouter = {
  // ...existing
  analytics: analyticsRouter,
  insights: insightsRouter,
};
```

---

## 3. UI Component Architecture

### 3.1 Analytics Page Route

**File:** `apps/web/src/app/(app)/surveys/[surveyId]/analytics/page.tsx` (NEW)

Server component entry point using Suspense for streaming:
- `AnalyticsOverview` - Summary stats cards (streamed)
- `AnalyticsTabs` - Tabbed interface (client component)

### 3.2 Chart Components

**Directory:** `apps/web/src/components/charts/` (NEW)

| Component | Purpose |
|-----------|---------|
| `chart-provider.tsx` | Shared context for theming, formatters |
| `bar-chart.tsx` | Horizontal/vertical bar charts |
| `line-chart.tsx` | Time-series trends |
| `pie-chart.tsx` | Choice distributions |
| `nps-gauge.tsx` | NPS score visualization |
| `word-cloud.tsx` | Keyword cloud for text analysis |

**Pattern:** Compound components with `ChartProvider` wrapper, using Recharts primitives with shadcn/ui theming.

### 3.3 Analytics Components

**Directory:** `apps/web/src/components/analytics/` (NEW)

| Component | Purpose |
|-----------|---------|
| `analytics-overview.tsx` | 3 summary stat cards |
| `analytics-tabs.tsx` | Tab navigation (Charts/Responses/Insights/Demographics) |
| `question-charts.tsx` | Per-question visualizations |
| `response-table.tsx` | Filterable/searchable response list |
| `ai-insights.tsx` | Summary, sentiment, keyword cloud |
| `demographics-panel.tsx` | Device/browser/country breakdowns |

### 3.4 Component Composition Pattern

Follow existing patterns from `survey-builder/`:
- Client components for interactivity (`"use client"`)
- TanStack Query with `orpc.xxx.queryOptions()`
- Loading states with `Skeleton` components
- Error handling with `Sonner` toasts

---

## 4. AI Integration Strategy

### 4.1 LLM Service Layer

**File:** `packages/api/src/services/llm.ts` (NEW)

- Use OpenAI SDK with Groq's compatible endpoint (`baseURL: "https://api.groq.com/openai/v1"`)
- Structured output with Zod schemas for consistent response parsing
- Models: `llama-3.3-70b-versatile` (recommended) or `mixtral-8x7b-32768`
- Synchronous processing with loading states in UI
- Error handling with retries for rate limits

### 4.2 Caching Strategy

| Data Type | TTL | Storage |
|-----------|-----|---------|
| Summary stats | 5 min | `analyticsSnapshot` table |
| Question analytics | 10 min | `analyticsSnapshot` table |
| AI analysis | 24 hours | `aiAnalysis` table |
| Trends | 30 min | `analyticsSnapshot` table |

### 4.3 Processing Pattern

**Synchronous with UI loading states:**
1. User opens AI Insights tab
2. Frontend shows loading spinner
3. API checks cache → if expired, calls Groq API
4. Returns results (typically 1-3 seconds)
5. Cache stored for subsequent requests

For surveys with >500 text responses, consider batching the LLM calls or showing progressive results.

---

## 5. Critical Files to Modify/Create

### New Files (10)
1. `packages/db/src/schema/analytics.ts`
2. `packages/api/src/routers/analytics.ts`
3. `packages/api/src/routers/insights.ts`
4. `packages/api/src/services/llm.ts`
5. `apps/web/src/app/(app)/surveys/[surveyId]/analytics/page.tsx`
6. `apps/web/src/components/charts/index.tsx` (exports)
7. `apps/web/src/components/charts/chart-provider.tsx`
8. `apps/web/src/components/charts/bar-chart.tsx`
9. `apps/web/src/components/charts/line-chart.tsx`
10. `apps/web/src/components/analytics/` (6 components)

### Modified Files (6)
1. `packages/db/src/schema/survey.ts` - Add indexes
2. `packages/api/src/routers/index.ts` - Register new routers
3. `packages/api/package.json` - Add `openai` dependency
4. `packages/env/src/server.ts` - Add `GROQ_API_KEY`, `GROQ_MODEL` env vars
5. `apps/web/package.json` - Add recharts, @tanstack/react-table
6. `apps/web/.env.example` - Add Groq environment variable documentation

---

## 6. Implementation Sequence

### Phase 4.1: Foundation
1. Install Recharts and TanStack Table
2. Create `analytics.ts` schema with indexes
3. Run `pnpm db:push` to apply schema changes
4. Create `analytics.ts` router with `getSummary` endpoint
5. Create basic chart components (Bar, Line)

### Phase 4.2: Dashboard UI
1. Create analytics page route
2. Build `AnalyticsOverview` with summary cards
3. Build `AnalyticsTabs` navigation
4. Build `QuestionCharts` for each question type
5. Build `DemographicsPanel`

### Phase 4.3: Response Table
1. Add TanStack Table
2. Build `ResponseTable` component
3. Add filtering (status), search, pagination
4. Add export functionality

### Phase 4.4: AI Integration
1. Add LLM environment variables to env package
2. Create `llm.ts` service layer
3. Create `insights.ts` router
4. Build `AIInsights` component
5. Build `WordCloud` component

---

## 7. Verification Plan

1. **Unit Testing:**
   - Test analytics aggregation queries with sample data
   - Test LLM service with mock responses

2. **Integration Testing:**
   - Create test survey with responses
   - Verify all chart renderers display correctly
   - Verify response table filtering/search
   - Verify AI insights generation

3. **Manual Testing:**
   ```bash
   pnpm dev:web
   # Navigate to /surveys/[surveyId]/analytics
   # Verify summary cards load
   # Check each tab (Charts, Responses, Insights, Demographics)
   # Test response filtering and search
   ```

4. **Performance Testing:**
   - Test with 1000+ responses
   - Verify query performance with indexes
   - Check AI analysis caching works

---

## 8. Design Decisions (Resolved)

- **LLM Provider:** Groq API (OpenAI-compatible endpoint)
- **Processing Mode:** Synchronous with UI loading states
- **Chart Colors:** CSS variables (`--chart-1` through `--chart-5`) for theme consistency


If you need specific details from before exiting plan mode (like exact code snippets, error messages, or content you generated), read the full transcript at: /Users/orksys/.claude/projects/-Users-orksys-Projects-orksys-survey/bb116ac3-a4d1-4cdf-aef4-eb8ef6416002.jsonl

If this plan can be broken down into multiple independent tasks, consider using the TeamCreate tool to create a team and parallelize the work.

---

[Request interrupted by user]

---

hang on, why we drop the z.infer for a regular type? it should be kept, use zod properly and infer the types.

---

## Error Type
Console Error

## Error Message
[Table] Column with id 'answers' does not exist.


    at cell (src/components/analytics/response-table.tsx:126:25)
    at <unknown> (src/components/analytics/response-table.tsx:235:24)
    at Array.map (<anonymous>:null:null)
    at <unknown> (src/components/analytics/response-table.tsx:233:34)
    at Array.map (<anonymous>:null:null)
    at ResponseTable (src/components/analytics/response-table.tsx:231:34)
    at AnalyticsTabs (src/components/analytics/analytics-tabs.tsx:44:5)
    at AnalyticsPage (src/app/(app)/surveys/[surveyId]/analytics/page.tsx:74:6)

## Code Frame
  124 |             header: q.title.length > 20 ? `${q.title.slice(0, 20)}...` : q.title,
  125 |             cell: ({ row }: { row: { getValue: (key: string) => unknown } }) => {
> 126 |                 const answers = row.getValue("answers") as Record<string, unknown>;
      |                                     ^
  127 |                 const value = answers?.[q.id];
  128 |                 if (value === null || value === undefined) {
  129 |                     return <span className="text-muted-foreground">—</span>;

Next.js version: 16.1.6 (Turbopack)

---

its difficult to access analytics page for a survey, can you add an easy link to it within the entires present on the surveys page as well as the menu option next to Survey Settings?

---

[Request interrupted by user]

---

use the Link, do not use <a>

---

commit this and push

---

This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Analysis:
Let me chronologically analyze the conversation:

1. **Initial Request**: User asked to implement Phase 4 of the survey platform - Analytics & Insights. The plan included:
   - New dependencies (recharts, @tanstack/react-table, openai)
   - Database schema extensions (analyticsSnapshot, aiAnalysis tables)
   - API layer (analytics router, insights router)
   - UI components (charts, analytics page)
   - AI integration with Groq API

2. **Implementation Process**:
   - Installed dependencies (recharts, react-table, openai)
   - Created analytics.ts schema with caching tables
   - Added composite indexes to survey.ts for efficient aggregation
   - Added GROQ_API_KEY and GROQ_MODEL env vars
   - Created LLM service layer (packages/api/src/services/llm.ts)
   - Created analytics router (packages/api/src/routers/analytics.ts)
   - Created insights router (packages/api/src/routers/insights.ts)
   - Created chart components (bar-chart, line-chart, pie-chart, nps-gauge, word-cloud)
   - Created analytics page and components

3. **Errors Encountered**:
   - Lucide icons didn't have SentimentSatisfied/SentimentNeutral/SentimentDissatisfied - fixed by using Smile/Meh/Frown
   - Zod type inference issue with KeywordsResponse - user corrected me to use z.infer properly
   - Import path issues with @orksys-survey/db/schema - fixed to use @orksys-survey/db/schema/analytics and @orksys-survey/db/schema/survey
   - Env import path issue - fixed to @orksys-survey/env/server
   - TanStack Table column error - fixed by using accessorFn instead of accessorKey for nested data
   - Next.js 16 Link strict type checking - fixed by using URL object format with pathname

4. **User Feedback**:
   - "hang on, why we drop the z.infer for a regular type? it should be kept, use zod properly and infer the types." - I fixed this by keeping z.infer and creating a separate KeywordsWithWeights type
   - "use the Link, do not use <a>" - I fixed by using Link with URL object format

5. **Final Request**: Add easy links to analytics page from surveys list and builder header

6. **Final Action**: Committed and pushed all changes

Summary:
1. Primary Request and Intent:
   The user requested implementation of Phase 4 of the survey platform: Analytics & Insights Dashboard. This involved:
   - Installing recharts, @tanstack/react-table, and openai dependencies
   - Creating database schema for analytics caching (analyticsSnapshot, aiAnalysis tables)
   - Building API routers for analytics and AI-powered insights
   - Creating chart components and analytics UI pages
   - Integrating Groq API (OpenAI-compatible) for AI text analysis
   - Adding easy navigation links to analytics from surveys list and builder header

2. Key Technical Concepts:
   - Recharts for data visualization (bar, line, pie charts)
   - TanStack Table for response data tables
   - Groq API with OpenAI SDK for LLM integration
   - Zod schemas for structured LLM output parsing
   - Drizzle ORM for database schema and queries
   - Caching strategy with TTL (5min for summary, 24hr for AI analysis)
   - oRPC router patterns with organizationProcedure
   - Next.js 16 App Router with strict Link type checking

3. Files and Code Sections:
   - `packages/db/src/schema/analytics.ts` (NEW)
     - Caching tables for pre-computed analytics and AI analysis results
     - Includes type definitions for SummaryAnalytics, QuestionAnalytics, DemographicsData, etc.

   - `packages/db/src/schema/survey.ts` (MODIFIED)
     - Added composite indexes for efficient analytics queries:
     ```typescript
     index("response_surveyId_completedAt_idx").on(table.surveyId, table.completedAt),
     index("response_surveyId_isComplete_idx").on(table.surveyId, table.isComplete),
     index("response_surveyId_createdAt_idx").on(table.surveyId, table.createdAt),
     ```

   - `packages/api/src/services/llm.ts` (NEW)
     - Groq API integration using OpenAI SDK
     - Functions: generateTextSummary, analyzeSentiment, extractKeywords
     - Uses z.infer for type safety with separate KeywordsWithWeights type

   - `packages/api/src/routers/analytics.ts` (NEW)
     - Endpoints: getSummary, getQuestionAnalytics, getTrends, getDemographics, exportResponses
     - Caching with TTL using analyticsSnapshot table

   - `packages/api/src/routers/insights.ts` (NEW)
     - Endpoints: getTextSummary, getSentimentAnalysis, getKeywords, refreshAnalysis, getStatus
     - 24-hour TTL for AI analysis caching

   - `apps/web/src/components/charts/` (NEW)
     - bar-chart.tsx, line-chart.tsx, pie-chart.tsx, nps-gauge.tsx, word-cloud.tsx
     - chart-provider.tsx with shared context and colors

   - `apps/web/src/components/analytics/` (NEW)
     - analytics-overview.tsx, analytics-tabs.tsx, question-charts.tsx, response-table.tsx, ai-insights.tsx, demographics-panel.tsx

   - `apps/web/src/app/(app)/surveys/[surveyId]/analytics/page.tsx` (NEW)
     - Main analytics page with summary cards and tabbed interface

   - `apps/web/src/app/(app)/surveys/page.tsx` (MODIFIED)
     - Added Analytics link to dropdown menu:
     ```typescript
     <DropdownMenuItem>
       <Link
         href={{ pathname: `/surveys/${survey.id}/analytics` }}
         className="flex items-center"
       >
         <BarChart3 className="mr-2 h-4 w-4" />
         Analytics
       </Link>
     </DropdownMenuItem>
     ```

   - `apps/web/src/components/survey-builder/builder-header.tsx` (MODIFIED)
     - Added Analytics menu option next to Survey Settings in dropdown

4. Errors and fixes:
   - **Lucide icons not found**: SentimentSatisfied, SentimentNeutral, SentimentDissatisfied don't exist in lucide-react
     - Fixed by using Smile, Meh, Frown icons instead
   
   - **Zod type inference**: I incorrectly created a manual type instead of using z.infer
     - User feedback: "hang on, why we drop the z.infer for a regular type? it should be kept, use zod properly and infer the types."
     - Fixed by keeping z.infer and creating separate KeywordsWithWeights type:
     ```typescript
     export type KeywordsResponse = z.infer<typeof KeywordsSchema>;
     export type KeywordsWithWeights = {
       keywords: Array<z.infer<typeof KeywordsSchema>['keywords'][number] & { weight: number }>;
     };
     ```
   
   - **Import path issues**: @orksys-survey/db/schema didn't exist
     - Fixed by using @orksys-survey/db/schema/analytics and @orksys-survey/db/schema/survey
   
   - **Env import issue**: @orksys-survey/env didn't resolve
     - Fixed by using @orksys-survey/env/server (per package.json exports)
   
   - **TanStack Table column error**: "Column with id 'answers' does not exist"
     - Fixed by using accessorFn instead of accessorKey:
     ```typescript
     ...questions.slice(0, 3).map((q) => ({
       id: `answer-${q.id}`,
       accessorFn: (row: ResponseRow) => row.answers?.[q.id],
       // ...
     }))
     ```
   
   - **Next.js 16 Link strict type checking**: Dynamic route template literals failed type checking
     - User feedback: "use the Link, do not use <a>"
     - Fixed by using URL object format:
     ```typescript
     <Link href={{ pathname: `/surveys/${survey.id}/analytics` }}>
     ```

5. Problem Solving:
   - Successfully implemented complete analytics dashboard with AI insights
   - Resolved all TypeScript compilation errors
   - Fixed runtime error with TanStack Table column access
   - Bypassed Next.js 16 strict Link type checking with URL object format

6. All user messages:
   - "Implement the following plan: [Phase 4 plan]"
   - "hang on, why we drop the z.infer for a regular type? it should be kept, use zod properly and infer the types."
   - "its difficult to access analytics page for a survey, can you add an easy link to it within the entires present on the surveys page as well as the menu option next to Survey Settings?"
   - "use the Link, do not use <a>"
   - "commit this and push"

7. Pending Tasks:
   - None - all requested tasks have been completed and pushed

8. Current Work:
   The final task was adding easy navigation links to the analytics page and committing/pushing. This was completed:
   - Added Analytics link to surveys list dropdown menu
   - Added Analytics menu option to survey builder header dropdown
   - Committed: `feat: add analytics dashboard and AI-powered insights (Phase 4)`
   - Pushed to origin/main

9. Optional Next Step:
   No pending next steps. All Phase 4 tasks have been completed and pushed. The user's final request "commit this and push" has been fulfilled.

If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: /Users/orksys/.claude/projects/-Users-orksys-Projects-orksys-survey/98d352c2-055e-47e4-a234-deaa7d4dd2bf.jsonl

---

I would like to plan comprehensive refactoring of all React components (that are NOT of shadcn OR NOT the page.tsx, error.tsx or layout.tsx) that have more than 120 lines of code, using the vercel composition, next and react best practices skills (3 skills) to refactor all component into foucused, single responsibility concise and composable building block components that allow creating more complex structures when working together (combined) with logic extrated either into conext or custom hooks. Think carefuly and deeply as there is no time limit to this task, it is vital to find as many complex components in the codebase right now in order to address them.

Requirements:
use the skills available, use mcp for docs and websearch, be very diligent instead of being fast

---

Base directory for this skill: /Users/orksys/.claude/skills/vercel-composition-patterns

# React Composition Patterns

Composition patterns for building flexible, maintainable React components. Avoid
boolean prop proliferation by using compound components, lifting state, and
composing internals. These patterns make codebases easier for both humans and AI
agents to work with as they scale.

## When to Apply

Reference these guidelines when:

- Refactoring components with many boolean props
- Building reusable component libraries
- Designing flexible component APIs
- Reviewing component architecture
- Working with compound components or context providers

## Rule Categories by Priority

| Priority | Category                | Impact | Prefix          |
| -------- | ----------------------- | ------ | --------------- |
| 1        | Component Architecture  | HIGH   | `architecture-` |
| 2        | State Management        | MEDIUM | `state-`        |
| 3        | Implementation Patterns | MEDIUM | `patterns-`     |
| 4        | React 19 APIs           | MEDIUM | `react19-`      |

## Quick Reference

### 1. Component Architecture (HIGH)

- `architecture-avoid-boolean-props` - Don't add boolean props to customize
  behavior; use composition
- `architecture-compound-components` - Structure complex components with shared
  context

### 2. State Management (MEDIUM)

- `state-decouple-implementation` - Provider is the only place that knows how
  state is managed
- `state-context-interface` - Define generic interface with state, actions, meta
  for dependency injection
- `state-lift-state` - Move state into provider components for sibling access

### 3. Implementation Patterns (MEDIUM)

- `patterns-explicit-variants` - Create explicit variant components instead of
  boolean modes
- `patterns-children-over-render-props` - Use children for composition instead
  of renderX props

### 4. React 19 APIs (MEDIUM)

> **⚠️ React 19+ only.** Skip this section if using React 18 or earlier.

- `react19-no-forwardref` - Don't use `forwardRef`; use `use()` instead of `useContext()`

## How to Use

Read individual rule files for detailed explanations and code examples:

```
rules/architecture-avoid-boolean-props.md
rules/state-context-interface.md
```

Each rule file contains:

- Brief explanation of why it matters
- Incorrect code example with explanation
- Correct code example with explanation
- Additional context and references

## Full Compiled Document

For the complete guide with all rules expanded: `AGENTS.md`

---

Base directory for this skill: /Users/orksys/.claude/skills/vercel-react-best-practices

# Vercel React Best Practices

Comprehensive performance optimization guide for React and Next.js applications, maintained by Vercel. Contains 57 rules across 8 categories, prioritized by impact to guide automated refactoring and code generation.

## When to Apply

Reference these guidelines when:
- Writing new React components or Next.js pages
- Implementing data fetching (client or server-side)
- Reviewing code for performance issues
- Refactoring existing React/Next.js code
- Optimizing bundle size or load times

## Rule Categories by Priority

| Priority | Category | Impact | Prefix |
|----------|----------|--------|--------|
| 1 | Eliminating Waterfalls | CRITICAL | `async-` |
| 2 | Bundle Size Optimization | CRITICAL | `bundle-` |
| 3 | Server-Side Performance | HIGH | `server-` |
| 4 | Client-Side Data Fetching | MEDIUM-HIGH | `client-` |
| 5 | Re-render Optimization | MEDIUM | `rerender-` |
| 6 | Rendering Performance | MEDIUM | `rendering-` |
| 7 | JavaScript Performance | LOW-MEDIUM | `js-` |
| 8 | Advanced Patterns | LOW | `advanced-` |

## Quick Reference

### 1. Eliminating Waterfalls (CRITICAL)

- `async-defer-await` - Move await into branches where actually used
- `async-parallel` - Use Promise.all() for independent operations
- `async-dependencies` - Use better-all for partial dependencies
- `async-api-routes` - Start promises early, await late in API routes
- `async-suspense-boundaries` - Use Suspense to stream content

### 2. Bundle Size Optimization (CRITICAL)

- `bundle-barrel-imports` - Import directly, avoid barrel files
- `bundle-dynamic-imports` - Use next/dynamic for heavy components
- `bundle-defer-third-party` - Load analytics/logging after hydration
- `bundle-conditional` - Load modules only when feature is activated
- `bundle-preload` - Preload on hover/focus for perceived speed

### 3. Server-Side Performance (HIGH)

- `server-auth-actions` - Authenticate server actions like API routes
- `server-cache-react` - Use React.cache() for per-request deduplication
- `server-cache-lru` - Use LRU cache for cross-request caching
- `server-dedup-props` - Avoid duplicate serialization in RSC props
- `server-serialization` - Minimize data passed to client components
- `server-parallel-fetching` - Restructure components to parallelize fetches
- `server-after-nonblocking` - Use after() for non-blocking operations

### 4. Client-Side Data Fetching (MEDIUM-HIGH)

- `client-swr-dedup` - Use SWR for automatic request deduplication
- `client-event-listeners` - Deduplicate global event listeners
- `client-passive-event-listeners` - Use passive listeners for scroll
- `client-localstorage-schema` - Version and minimize localStorage data

### 5. Re-render Optimization (MEDIUM)

- `rerender-defer-reads` - Don't subscribe to state only used in callbacks
- `rerender-memo` - Extract expensive work into memoized components
- `rerender-memo-with-default-value` - Hoist default non-primitive props
- `rerender-dependencies` - Use primitive dependencies in effects
- `rerender-derived-state` - Subscribe to derived booleans, not raw values
- `rerender-derived-state-no-effect` - Derive state during render, not effects
- `rerender-functional-setstate` - Use functional setState for stable callbacks
- `rerender-lazy-state-init` - Pass function to useState for expensive values
- `rerender-simple-expression-in-memo` - Avoid memo for simple primitives
- `rerender-move-effect-to-event` - Put interaction logic in event handlers
- `rerender-transitions` - Use startTransition for non-urgent updates
- `rerender-use-ref-transient-values` - Use refs for transient frequent values

### 6. Rendering Performance (MEDIUM)

- `rendering-animate-svg-wrapper` - Animate div wrapper, not SVG element
- `rendering-content-visibility` - Use content-visibility for long lists
- `rendering-hoist-jsx` - Extract static JSX outside components
- `rendering-svg-precision` - Reduce SVG coordinate precision
- `rendering-hydration-no-flicker` - Use inline script for client-only data
- `rendering-hydration-suppress-warning` - Suppress expected mismatches
- `rendering-activity` - Use Activity component for show/hide
- `rendering-conditional-render` - Use ternary, not && for conditionals
- `rendering-usetransition-loading` - Prefer useTransition for loading state

### 7. JavaScript Performance (LOW-MEDIUM)

- `js-batch-dom-css` - Group CSS changes via classes or cssText
- `js-index-maps` - Build Map for repeated lookups
- `js-cache-property-access` - Cache object properties in loops
- `js-cache-function-results` - Cache function results in module-level Map
- `js-cache-storage` - Cache localStorage/sessionStorage reads
- `js-combine-iterations` - Combine multiple filter/map into one loop
- `js-length-check-first` - Check array length before expensive comparison
- `js-early-exit` - Return early from functions
- `js-hoist-regexp` - Hoist RegExp creation outside loops
- `js-min-max-loop` - Use loop for min/max instead of sort
- `js-set-map-lookups` - Use Set/Map for O(1) lookups
- `js-tosorted-immutable` - Use toSorted() for immutability

### 8. Advanced Patterns (LOW)

- `advanced-event-handler-refs` - Store event handlers in refs
- `advanced-init-once` - Initialize app once per app load
- `advanced-use-latest` - useLatest for stable callback refs

## How to Use

Read individual rule files for detailed explanations and code examples:

```
rules/async-parallel.md
rules/bundle-barrel-imports.md
```

Each rule file contains:
- Brief explanation of why it matters
- Incorrect code example with explanation
- Correct code example with explanation
- Additional context and references

## Full Compiled Document

For the complete guide with all rules expanded: `AGENTS.md`

---

[Request interrupted by user for tool use]